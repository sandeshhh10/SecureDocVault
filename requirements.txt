# Secure-Doc — Python dependencies
# Install with: pip install -r requirements.txt

flask>=3.0.0
pycryptodome>=3.20.0
reportlab>=4.0.0

# Testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
coverage>=7.0.0

# Standard library modules used (no install needed):
#   hashlib, os, json, sqlite3, uuid, secrets, hmac,
#   datetime, textwrap, re, random, typing, unittest
